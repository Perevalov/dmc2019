Выводы по анализу данных:

/// НАНы ///

Нанов нет

/// Сравнение распределений на train и test ///

Распределения не совпадают у (из за выбросов, если их убрать то всё ок): 
- valuePerSecond
- scannedLineItemsPerSecond

/// Выбросы ///

Выбросы детектируются на:

- valuePerSecond
- scannedLineItemsPerSecond

Для удаления выбросов на этих фичах заменяем все значения, больше 1 на средние этой фичи, после этого распределения на трейне и тесте совпадают

- lineItemVoidsPerPosition

/// Логичность данных ///

В тесте имеются примеры с полями grandTotal и valuePerSecond = 0 (одни и теже), это странно

Проверка фичи lineItemVoidsPerPosition через формулу (lineItemVoids/scannedLineItemsPerSecond*totalScanTimeInSeconds) не совпадает с реальными значениями